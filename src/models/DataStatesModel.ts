export type DataStatesModel = [
  {
    code: number;
    uf: string;
    description: string;
    region: string;
  }
];

export type DataStateModel = {
  code: number;
  UF: string;
  description: string;
  region: string;
};
